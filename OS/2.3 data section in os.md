
Data Section is where global and static variabes are stored

static -> same memory address
These variables are referenced directly through memory address
Bp is static for data section

we can do read only and read/write
this causes cache invalidation -> when you change data while another core is reading it, so other core should read back in mem.

All functions can access it.

it has a fixed size

