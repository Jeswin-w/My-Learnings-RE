# Virtual Memory

A layer above physical memory

Limitations of physical memory

- Fragmentation

Memory not stored in order but fragmented in different areas. this leads to shortage of space for a large memory.

Process need contiguous pace in ram 

eg ) p1 , p2 (Large), p3 are running 

p1 and p3 are done . p4 (large) doesn't have space as p2 occupies middle. there is enough space but it is fragmented.

    External vs Internal Fragmentation
    Memory allocated in blocks for each process
    - External
    there is space for next process but there is no continuous space
    Variable block size
    -Internal
    Fixed BLock size
    Wasted unused space

Virtual memory
uses a fixed block size 4kb in OS called paging
each process has a virtual address space
we map the virtual page to physical page
many virtual page is mapped to 1 physical

Page Table
Virtual address needs translation
each process has a page table
it is stored in memory
It stores page number instad of address, a address is large (64bit)

Shared memory


why we need it?
if same program is started 5 times
5 text section of same code is a waste
 
 With virtual memory all vertial address point to the same physical memory of the same code

Most processes use libraries (like input etc)
OS loads it once and maps it to different virtual memory

